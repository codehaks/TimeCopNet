<div class="navbar fixed-top navbar-expand-lg navbar-dark bg-primary text-light">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>


        <a class="navbar-brand" asp-page="/Index" asp-area="">TimeCop</a>

        <div class="collapse navbar-collapse navbarTogglerDemo01"  >
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" asp-page="/index" asp-area="">Home</a>
                </li>

              
                @if (User.Identity.IsAuthenticated)
                {
                    <li>
                        <a class="nav-link" asp-page="/index"  asp-area="user">My Account</a>
                    </li>
                }

                @if (User.IsInRole("admin"))
                {
                    <li><a class="nav-link" asp-action="index" asp-controller="home" asp-area="admin">Admin</a></li>
                }

            </ul>
        </div>

        @await Html.PartialAsync("_LoginPartial")

    </div>
</div>